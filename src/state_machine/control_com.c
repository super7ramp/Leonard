#include "control_com.h"

void* thread_com(void* arg)
{

	//initialisation de la connection UDP entre le logiciel embarqué et l'API

	//initialisation de la connection UDP entre le logiciel embarqué et l'IHM 

	while(1)
	{
		takeOff_();
		printf("bonjoirs\n");
	}  
}
